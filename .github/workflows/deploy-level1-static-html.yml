name: "Skenario 1 - Level 1 - Deploy Static Site"

# Trigger: push ke branch main, hanya jika folder app/ level1 berubah
on:
  push:
    branches: [ main ]
    paths:
      - 'skenario1-github-actions-vps-proxycommand/level1-static-html/app/**'
  workflow_dispatch:
jobs:
  deploy:
    name: Deploy static site ke VM-A
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kode
        uses: actions/checkout@v4

      - name: Install cloudflared
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o cloudflared.deb
          sudo dpkg -i cloudflared.deb
          cloudflared --version

      - name: Install sshpass
        run: sudo apt-get update -y && sudo apt-get install -y sshpass

      - name: Siapkan SSH config (ProxyCommand via Cloudflare Access, tanpa SSH key)
        env:
          CF_ACCESS_HOSTNAME: ${{ secrets.CF_ACCESS_HOSTNAME }}
        run: |
          mkdir -p ~/.ssh
          cat >> ~/.ssh/config <<EOC
          Host vm-a
            HostName ${CF_ACCESS_HOSTNAME}
            User ${{ secrets.VPS_USERNAME }}
            ProxyCommand cloudflared access ssh --hostname %h
            StrictHostKeyChecking no
            UserKnownHostsFile=/dev/null
          EOC

      - name: Deploy file statis ke VM-A
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
        run: |
          ssh vm-a "sudo mkdir -p /var/www/level1-static && sudo chown \$USER:\$USER /var/www/level1-static"
          sshpass -e scp -o StrictHostKeyChecking=no \
            skenario1-github-actions-vps-proxycommand/level1-static-html/app/index.html \
            skenario1-github-actions-vps-proxycommand/level1-static-html/app/style.css \
            vm-a:/var/www/level1-static/

      - name: Reload Nginx di VM-A
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
        run: |
          sshpass -e ssh vm-a "sudo systemctl reload nginx"
